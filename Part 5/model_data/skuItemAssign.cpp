#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
  srand(time(NULL));
  ifstream chair_file("chairs.csv");
  ifstream table_file("tables.csv");
  ifstream desk_file("desks.csv");
  ifstream stool_file("stools.csv");
  ifstream cabinet_file("cabinets.csv");
  ifstream bedframe_file("bedframes.csv");
  
  
  //Take in "chairs.csv" and ouput itme values
  
  //Take in "tables.csv" and output item values
  
  //Take in "desks.csv" and output item values
  
  //Take in "stools.csv" and output item values
  
  //Take in "cabinets.csv" and output item values
  
  //Take in "bedframes.csv" and output item values
  
  return 0;
}
